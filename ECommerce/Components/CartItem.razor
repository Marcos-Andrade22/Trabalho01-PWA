@code {
    [Parameter] public Produto Produto { get; set; }
}

<div class="flex flex-row bg-white p-4 w-xl rounded-lg shadow-md hover:shadow-lg gap-2">
    <!-- Imagem do produto -->
    <img src="@Produto.ImagemUrl" alt="@Produto.Nome" class="w-32 h-32 object-contain rounded-md" />

    <!-- Informações do produto -->
    <div class="flex flex-col justify-between flex-grow">
        <div>
            <h3 class="text-lg font-semibold text-gray-800">@Produto.Nome</h3>
            <p class="text-gray-500 text-sm">@Produto.Descricao</p>
        </div>

        <div class="flex items-center justify-between mt-2">
            <p class="text-lg font-bold text-green-500">R$ @Produto.Preco.ToString("F2")</p>
        </div>
    </div>
</div>
